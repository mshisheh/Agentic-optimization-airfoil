# Base case scaffold

This directory was created for the OpenFOAM base case.

Required folders:
- `0/`
- `system/`
- `constant/`

Populate these folders by copying a tutorial case (e.g., `airfoil2D` or a `simpleFoam` tutorial) from your local OpenFOAM installation.
